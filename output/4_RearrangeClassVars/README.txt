Here I rearranged the elements of ClassVars.csv, hoping that I can shunt all unnecesary files to the far end. Unfortunately this raises an error, as CDmetaPOP_PreProcess.py appears to appends elements of the cs into a function, instead of using pandas. 

This limits the degree to which I can naively simplify elements. 

I think the sensible idea now is just to set all extraneous values to 0 (or N, which sometimes stands for 'Don't count this'). 

This has been achieved for ClassVars. Now to start with the rest.
